var sinon = require('sinon'),
    assert = require('assert'),
    expect = require('chai').expect;

//Require file to be tested..

describe('The unit test', function () {

    it ('should pass', function () {
        expect(true).to.equals(true)
    });
});